import { useParams } from "react-router-dom";

function ApplicationDetails() {

  const { id } = useParams();

  return (
    <div className="page">

      <h2>Application Details</h2>

      {/* MOVE YOUR FULL EXISTING TEST / INTERVIEW / HR / OFFER / ONBOARDING UI HERE */}
      {/* Nothing removed — just paste from your current dashboard */}

    </div>
  );
}

export default ApplicationDetails;
